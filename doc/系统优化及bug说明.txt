0、datagrid分页问题现未调试，需要修正

1、跨数据库：
	所有的原生sql语句都需换成hql语句，为了适应底层框架跨数据库

2、hibernate关联查询优化

3、日志：
	日志输出到文件中时，文件的路径现使用的是绝对路径，需要换成相对路径以解决项目移植后日志输出到文件找不到文件路径问题

4、事务隔离级别：
	在同一个Action方法中调用两个不同service的接口时，第二个service接口的事务未提交，也不报异常，
	如：在UserAction中的addUserToUserGroup方法中先根据userGroupId查询用户组对象后修改该对象，
	再调用service的更新接口该对象时，更新的service接口始终不提交事务，无法更新
	
5、编辑角色时，还得根据角色id查询角色得到持久化对象后才能重新赋值修改，此处应该直接就可以修改的，不知所以然

6、用户组管理详细信息查看和组织机构详细信息查看页面布局